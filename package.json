{
  "name": "horror-game",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "server": "node server/server.js",
    "build": "vite build && node scripts/obfuscate.js",
    "build:clean": "vite build",
    "preview": "vite preview",
    "start": "concurrently \"npm run dev\" \"npm run server\""
  },
  "dependencies": {
    "three": "^0.170.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "javascript-obfuscator": "^4.1.1",
    "vite": "^6.0.0"
  }
}
